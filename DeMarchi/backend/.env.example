# Configuração do banco local (desenvolvimento)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=controle_gastos

# Para Railway, use DATABASE_URL (será configurado automaticamente)
# DATABASE_URL=mysql://user:password@host:port/database

# JWT Secret
JWT_SECRET=sua_chave_secreta_aqui

# Ambiente
NODE_ENV=development

# Porta do servidor
PORT=3000
